<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stucom.alu2015018.p1_menuprincipal">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".jugar.JugarActivity" />
        <activity android:name=".ranking.RankingActivity" />
        <activity android:name=".ajustes.AjustesActivity" />
        <activity android:name=".gamep1.ChooseGame" />
        <activity android:name=".ranking.UserRankingDetails"></activity>
        <activity
            android:name=".gamep3.GameActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="landscape"></activity>
    </application>

</manifest>